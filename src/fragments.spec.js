import * as fragments from './fragments'

describe('Test fragments', () => {
  it('should be defined', () => {
    expect(fragments.articlesPageArticleMetadata).toBeDefined()
    expect(fragments.articleContent).toBeDefined()
  })
})
